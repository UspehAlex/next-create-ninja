import React from 'react';
import BlocksContainer from '../components/BlocksContainer';

const Marketing = () => {
  return (
    <BlocksContainer page="Marketing" />
  );
};

export default Marketing;
