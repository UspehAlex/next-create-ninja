import React from 'react';
import BlocksContainer from '../components/BlocksContainer';

const Promotion = () => {
  return (
    <BlocksContainer page="Promotion" />
  );
};

export default Promotion;
