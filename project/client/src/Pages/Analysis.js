import React from 'react';
import BlocksContainer from '../components/BlocksContainer';

const Analysis = () => {
  return (
    <BlocksContainer page="Analysis" />
  );
};

export default Analysis;
