import React from 'react';
import BlocksContainer from '../components/BlocksContainer';

const CreatingPictures = () => {
  return (
    <BlocksContainer page="Creating Pictures" />
  );
};

export default CreatingPictures;
